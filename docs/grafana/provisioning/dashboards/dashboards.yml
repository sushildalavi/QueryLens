apiVersion: 1
providers:
  - name: PlanTrace Dashboards
    orgId: 1
    folder: PlanTrace
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
